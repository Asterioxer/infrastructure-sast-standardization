exceptions:
  enabled: true

  approval_required: true

  allowed_types:
    - false_positive
    - legacy_code
    - temporary_risk_acceptance

  mandatory_fields:
    - owner
    - justification
    - expiry_date

  max_expiry_days: 90

  enforcement:
    block_if_missing_metadata: true
